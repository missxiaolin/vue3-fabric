/**
 * PSD文字属性解析
 */
import { Layer } from "ag-psd";
import { toRGBColorStr } from "@/utils/color";

/**
 * 转换Text元素
 * @param layer 图层信息
 * @param options 额外属性
 */
export function parseText(layer: any, canvasEditor: any, options = {}) {

}